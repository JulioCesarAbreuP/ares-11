class WORMStorage:
    """Adaptador para persistencia forense WORM (ej: Azure Blob con Lock Policy)."""
    async def persist_event(self, event: dict):
        # Aquí iría la lógica real de persistencia forense
        pass
